#include <stdio.h>
#include <unistd.h>

/**
 * main - Entry point
 * Description: Print base 16 numbers
 * Return: Always 0 (success)
 */

int main(void)
{

char sm;
char a = '0';
while (a <= '9')
{
putchar(a);
d++;
}
for (sm = 'a'; sm <= 'f'; sm++)
{
putchar(sm);
}

putchar('\n');

return (0);

}
